#include<stdio.h>

int main(){
    /*
    int x,y,z;

    z = x+y;

    printf("Enter the number of x and enter the number of y\n");
    scanf("%d %d", &x, &y);
    z = x + y;
    printf("The value of %d + %d equal %d\n",x,y,z); */
    char ch;

    printf("Enter the char:");
    scanf("%c", &ch);
    printf("The character you entered %c",ch);

    return 0;
}
